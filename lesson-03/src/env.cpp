#include <iostream>

int main(int argc, char** argv, char** env)
{
    for (size_t i = 0; i < 10; ++i)
    {
        std::cout << env[i] << std::endl;
    }
    return 0;

    int64_t arr[2000000];
}
